version: "2"
sql:
  - schema: "schema.sql"
    queries: "."
    engine: sqlite
    gen:
      go:
        package: "queries"
        out: "."
        sql_package: "database/sql"
        emit_db_tags: true
        emit_empty_slices: true
        emit_methods_with_db_argument: true
